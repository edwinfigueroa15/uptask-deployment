doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style 
            include ./email.css
    body
        h2 Confirma tu cuenta UpTask
        p Hola, ya casi estas inscrito solo da click en el siguiente boton.
        a(href=`${confirmUrl}`) Confirmar Cuenta
        p Si no puedes acceder a este password, visita #{confirmUrl}
        p Si no solicitaste este e-mail puede ignorarlo