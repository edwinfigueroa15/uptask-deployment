extends layout
include mixins/crearProyecto

block contenido
    .contenido-principal 
        h1 #{title}

        if errores
            each error in errores
                .alerta= error.msg
        
        +crearProyecto(proyecto)

        